class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
        int l1 = 0, l2 = 0, r1 = nums1.size(), r2 = nums2.size(), mid1, mid2;
        int idx = 
        while(l1 < r1 && l2 < r2){
            mid1 = (l1 + r1) / 2;
            mid2 = (l2 + r2) / 2;
            if(nums1[mid1] < )   
        }
    }
};