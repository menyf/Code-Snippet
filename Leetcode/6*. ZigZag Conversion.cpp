class Solution {
public:
    string convert(string s, int numRows) {
        string str[numRows];
        int len = s.length();
        bool odd = true;
        for(int i = 0; i < len; i++){
            
        }
    }
};